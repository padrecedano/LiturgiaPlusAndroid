package org.deiverbum.app.core.model.data

/**
 * Esta clase es una representación de la tabla `bible_homily_join` en la capa de datos externa.
 */
class BibleHomilyJoin {
    @JvmField
    var readingFK = 0

    @JvmField
    var homilyFK = 0
}