<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" /> <!-- Without this entry the folders will remain in-accessible in Android-10, even if WRITE_EXTERNAL_STORAGE as above is present. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />

    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:name="BaseApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiturgiaPlusNew">


        <activity
            android:name=".MainActivity"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <!--
                <action android:name="android.intent.action.VIEW" />
                -->

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SENDTO" />

                <data android:scheme="mailto" />

                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="true" />
        <meta-data
            android:name="google_analytics_adid_collection_enabled"
            android:value="false" />
        <meta-data
            android:name="firebase_crashlytics_collection_enabled"
            android:value="true" />
        <!--
        <service
            android:name=".core.media.service.TtsMediaService"
            android:foregroundServiceType="mediaPlayback" />

            <service
            android:name=".core.media.service.SimpleMediaService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
        <action android:name="androidx.media3.session.MediaSessionService" />

        <action android:name="androidx.media3.session.MediaLibraryService.SERVICE_INTERFACE" />
    </intent-filter>
</service>
        -->
        <service
            android:name=".core.media.service.TtsMediaService"
            android:exported="true"
            android:foregroundServiceType="mediaPlayback">
            <intent-filter>
                <!-- ESTA LÍNEA DEBE SER EXACTAMENTE ASÍ -->
                <action android:name="androidx.media3.session.MediaSessionService" />

                <action android:name="androidx.media3.session.MediaLibraryService.SERVICE_INTERFACE" />
            </intent-filter>
        </service>

        <service
            android:name=".feature.tts.TextToSpeechService"
            android:exported="false">  <!-- Generalmente false si solo tu app lo usa -->
            <!-- Si apuntas a Android 14 (API 34) o superior y es para reproducción multimedia -->
            <!-- android:foregroundServiceType="mediaPlayback" -->
        </service>
    </application>

</manifest>